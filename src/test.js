import React from 'react'

export default function Test(currentTrack,
    currentIndex,
    setCurrentIndex,
    total,) {
    console.log(currentTrack);
    console.log(currentIndex);
    console.log(total);

  return (
    <div>T</div>
  )
}
